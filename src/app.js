// when importing the font in JS the stats.json does gets updated in stats.json while watching
// disabled to demonstrate that it's not working when importing in an scss file.
// import './fonts/sriracha-regular.woff2';

document.addEventListener('DOMContentLoaded', () => {
    console.log('ready!');
});
